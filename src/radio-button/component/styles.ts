import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  wrap: {
    justifyContent: 'center',
    alignItems: 'center',
    position: 'relative',
  },
  dot: {
    position: 'absolute',
    alignSelf: 'center',
  },
  row: {
    flexDirection: 'row',
    alignItems: 'center',
  },
  labelStyle: {
    paddingLeft: 12,
    fontSize: 20,
  },
});
